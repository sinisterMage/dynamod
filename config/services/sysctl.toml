[service]
name = "sysctl"
supervisor = "early-boot"
exec = ["/sbin/sysctl", "-p"]
type = "oneshot"

[restart]
policy = "temporary"

[dependencies]
after = ["fstab-mount"]

[readiness]
type = "none"

[shutdown]
stop-signal = "SIGTERM"
stop-timeout = "3s"
